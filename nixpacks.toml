[providers]
providers = ["python"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.11"
TZ = "UTC"

[phases.setup]
aptPkgs = ["tzdata"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python main.py"
